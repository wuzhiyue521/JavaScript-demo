/**
 * 1. 开发环境: 构建工具
 */

/**
 * 1. grunt
 *
 * 2. gulp
 * 3. fis -> 百度
 *
 * 4. webpack
 */

/**
 * 1. npm -v 查看 npm 版本
 * 2. node -v 查看 node 版本
 * 3. pwd 查看当前文件的路径
 */

/**
 * 1. npm init 初始化 webpack (生成一个 package.json 文件)。
 * 2. cnpm i webpack@3.0.0 --save-dev  ( --save-dev 在开发环境中使用 -> devDependencies)
 * 3. cnpm i jquery@3.2.1 --save  (--save 项目线上运行使用 -> dependencies)
 * 4. cnpm i moment --save (全局过滤器工具)
 * 5. 在 package.json 文件中配置启动命令:
 *  "scripts": {
 *    "test": "echo \"Error: no test specified\" && exit 1",
 *    "start": "webpack"
 *  },
 */

/**
 * 在 app.js 文件中展示 jq 的使用方式。
 */